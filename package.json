{
  "name": "mission-swarm",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Lightweight swarm-simulation engine — persona reactions to input documents, streamed live.",
  "scripts": {
    "start": "bun src/index.ts",
    "test": "bun test",
    "typecheck": "bun x tsc --noEmit"
  },
  "dependencies": {
    "yaml": "^2.7.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  },
  "engines": {
    "bun": ">=1.1.0"
  }
}
